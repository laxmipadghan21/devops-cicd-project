apiVersion: v1

kind: Service

metadata:
  name: devops-cicd-service

spec:
  type: NodePort

  selector:
    app: devops-cicd-app

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007